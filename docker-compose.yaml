services:
  frontend:
    build:
      context: ./frontend
    env_file:
      - .env/frontend.env
    ports:
      - "80:80"
    depends_on:
      - api
  api:
    build:
      context: ./api
    env_file:
      - .env/api.env
    restart: always
    depends_on:
      - db
    volumes:
      - ./monitoring/prometheus-config:/app/monitoring/prometheus-config
      - ~/.ssh:/root/.ssh
  db:
    image: postgres:17
    env_file:
      - .env/db.env
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always
  redis:
    image: redis:7
    volumes:
        - redis_data:/data
    restart: always
  adminer:
    image: adminer
    ports:
      - "8080:8080"
    restart: always
    depends_on:
      - db
  monitoring:
    image: prom/prometheus:v3.7.3
    env_file:
      - .env/monitoring.env
    volumes:
      - ./monitoring/prometheus-config:/etc/prometheus/
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    restart: always
    depends_on:
      - api
    command:
      - "--config.file=/etc/prometheus/prometheus.yaml"
      - "--storage.tsdb.path=/prometheus/"
      - "--web.console.libraries=/etc/prometheus/console_libraries"
      - "--web.console.templates=/etc/prometheus/consoles"
  logging:
    image: grafana/grafana:9.0.0
    env_file:
      - .env/logging.env
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana-config:/etc/grafana/provisioning
    ports: 
      - "3001:3000"
    restart: always
    depends_on:
      - monitoring
volumes:
  db_data:
  prometheus_data:
  grafana_data:
  redis_data:
